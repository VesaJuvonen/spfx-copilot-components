import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');const required=['README.md','todo.md','Zava-Customer-Resolution-4-Minute-Keynote.md','Zava-Customer-Resolution-10-Minute-Business-Demo.md','Zava-Customer-Resolution-5-Minute-Technical-Demo.md','Zava-Customer-Resolution-Designer-Review.md','Zava-Customer-Resolution-Routing-Matrix.md','assets/sample.json','assets/asset-provenance.json','ux-review/evidence/visual-evidence.json'];const assert=(value,message)=>{if(!value)throw new Error(message)};for(const file of required)assert(fs.existsSync(path.join(root,file)),`Missing publication asset: ${file}`);const visual=JSON.parse(fs.readFileSync(path.join(root,'ux-review','evidence','visual-evidence.json'),'utf8'));assert(visual.totalIntents===21,'Visual evidence must cover 21 intents.');assert(visual.totalCaptures===75,'Visual evidence must contain 75 captures.');assert(visual.failureCount===0,'Visual evidence contains failures.');assert(visual.uniqueLayouts>=25,'Visual evidence lacks unique intent/workspace layouts.');const readme=fs.readFileSync(path.join(root,'README.md'),'utf8');for(const file of required.slice(2,7))assert(readme.includes(file),'README must link '+file);const charts=fs.readFileSync(path.join(root,'src','shared','visualizations','ServiceCharts.tsx'),'utf8');assert(charts.includes("from 'd3-geo'")&&charts.includes("from 'world-atlas/countries-110m.json'"),'Real offline map stack is not wired.');assert(charts.includes('<table'),'Charts require exact-value table equivalents.');
console.log(JSON.stringify({documents:required.length,visualCaptures:75,visualFailures:0,uniqueLayouts:visual.uniqueLayouts,d3Geo:true,tableEquivalents:true}));
