# Zava Revenue Deal Room - Creation Plan

This is the implementation tracker for the product and UX brief in [README.md](README.md). The
sample follows [agentic-creation-rules.md](agentic-creation-rules.md). No feature implementation,
dependency changes, component generation, or identity replacement begins until Phase 0 is approved.

## Status legend

- `[ ]` open work
- `[x]` validated work
- **IN PROGRESS** marks the one active implementation slice
- **BLOCKED: reason** identifies an external decision or prerequisite

## Progress (latest)

> Tracker reconciled after the premium UX redesign on 31 August 2026. All 21 final identities were generated
> through Yeoman and validate in one shared React 18 bundle. The deterministic graph contains 80 accounts,
> 260 contacts, and 120 opportunities; 6 focused domain/D3 geometry tests pass with zero warnings.
> Thirteen Playwright captures
> pass with no runtime or horizontal-overflow failures. Seven bundled portraits pass source/runtime
> provenance checks and ship as seven unique hashed media assets with no duplicates. The audited
> 341,417-byte `.sppkg` contains one 398,994-byte hashed JavaScript asset and one current agent ZIP.
> Purpose-specific lifecycle depth for
> every non-keynote intent, exhaustive all-intent accessibility evidence, session-store propagation, and
> authenticated tenant-host checks remain open and are not claimed complete. After this reconciliation,
> the tracker contains 87 validated items, 84 open items, and 2 external blockers.

## Validated milestones (latest)

- [x] Update `agentic-creation-rules.md` from this session: require competitor-informed workspace
  purpose matrices, unique dashboard layouts/operations, D3 stable-domain geometry tests, real offline
  maps, duplicate-module audits, early publication scaffolding, and generated release evidence.
- [x] Inspect the named Zava Innovation Hub reference at screenshot, React/style, media, provenance,
  responsive, dark-mode, visual-harness, and package-output levels before the premium redesign.
- [x] Replace the generic shared visual foundation with a domain-specific multi-stop navigation/hero
  gradient system, restrained elevation, stronger typography, polished evidence/milestone/scenario rows,
  and a responsive editorial composition in `src/shared/RevenueApp.tsx`.
- [x] Add seven correctly mapped Microsoft 365 demo persona portraits to headers, deal-team identity,
  stakeholder graph nodes, evidence sources, and milestone owners with initials fallback.
- [x] Add `assets/asset-provenance.json`, a typed media catalog, source copies, and
  `check:media`; validate 7 runtime portraits, 7 source copies, 0 hash mismatches, and the explicit
  pending public media-rights status.
- [x] Regenerate and inspect 10 Playwright captures after the redesign; validate 0 runtime errors,
  0 broken images, and 0 horizontal-overflow failures, including desktop, 390px mobile, and dark mode.
- [x] Detect and correct masked mobile clipping, full-screen Expand leakage, capability-layout leakage,
  dark hero contrast, metric spacing, and commercial-contour finish through pixel review.
- [x] ~~Audit the 276,290-byte pre-D3 package with one 214,815-byte JavaScript asset.~~ **Superseded by
  the visualization-led package below;** the earlier baseline had 7 unique portraits and no stale media.
- [x] Add D3-derived commercial curves, evidence-weighted pipeline bars, signed forecast waterfall,
  and an offline Natural Earth/TopoJSON regional opportunity map with exact labels and data tables.
- [x] Prove D3 scenario, forecast, pipeline, map, and relationship geometry with 3 focused tests; all
  6 discovered domain/geometry tests pass with 0 failures.
- [x] Compose complementary D3 analytical regions into all four operational full-screen lenses and
  reuse chart-appropriate map, pipeline, forecast, and commercial visuals inline.
- [x] Replace the generic shared full-screen composition with four role-owned dashboards: seller
  prioritization/customer moments, pursuit influence/evidence/close path, commercial scenario/exception/
  policy governance, and executive pipeline/forecast/geography/intervention.
- [x] Add and validate unique `my-deals-dashboard`, `deal-room-dashboard`,
  `commercial-desk-dashboard`, and `revenue-command-dashboard` layout identities in publication captures.
- [x] Regenerate 13 publication captures after D3 integration with 0 runtime, broken-image, or overflow
  failures; inspect map, pipeline, forecast, My Deals, Deal Room, and Revenue Command pixels.
- [x] Rebuild and audit the final unique-dashboard package: 341,417-byte `.sppkg`, one 398,994-byte
  hashed JavaScript asset, 7 unique packaged portraits, 0 duplicate media, 0 stale JavaScript, and
  1 current agent ZIP.

## Approach and sequencing

1. Lock the keynote story, immutable catalog, identities, routes, visual direction, and evidence plan.
2. Remove the untouched placeholder through an explicit cleanup step, then generate final identities
   with the supported SharePoint Yeoman generator. Never rename or repurpose the placeholder.
3. Install and validate the React 18, Fluent v9, Griffel, test, and catalog automation baseline.
4. Prove one coherent mock domain and the shared host boundary before scaling component bodies.
5. Deliver the keynote proof slice first: relationship orbit, commitment runway, commercial contour,
   exception decision, forecast evidence, and exact full-screen continuation.
6. Validate the connected Contoso journey before implementing the remaining catalog by operation family.
7. Add the capability explorer, complete full-screen lenses, visual evidence, demos, and audited package.

The keynote should use a focused 5-6 beat live path while the broader 20-tool catalog proves product
depth outside the main stage sequence. The implementation order is therefore evidence-driven, not table
order from the README.

## Phase 0 - Scope, design, and identity lock

### Brief reconciliation

- [x] Approve the 20 operational components plus
  `ExploreAgentCapabilities` as the immutable final catalog.
- [x] Approve the four full-screen lenses and every exact route in the README.
- [x] Approve Contoso Global Expansion / `ZDR-2042` as the connected hero record and confirm the mock
  story contains no customer-confidential or unsupported market claims.
- [x] Approve the three hero scenarios and the keynote-first proof slice.
- [ ] Decide whether the main-stage story is a 5-6 beat journey and the 10-beat choreography is the
  extended business demo.
- [x] Approve component-selection rationale: each current tool satisfies at least two promotion tests;
  move any lower-value intent to an internal route before generation.
- [x] Freeze ownership and negative-routing boundaries for overlapping pairs, especially account brief
  vs meeting preparation, qualification vs forecast, risk vs coaching, and simulation vs exception.

### UX direction

- [x] Approve the README textual design direction as the implementation authority and save current
  visual evidence under `assets/` for the
  inline keynote states and one for the Deal Room full-screen workspace, or explicitly approve the
  README's textual design system as the sole design authority.
- [x] Approve and implement the editorial visual direction: asymmetric evidence-led composition, strong vertical
  close runway, relationship orbit, restrained cards, and responsive 340 px behavior.
- [x] Approve signature-visual semantics and implement accessible equivalents for influence orbit, commitment
  runway, evidence ledger, commercial contour, and forecast bridge.
- [x] Reconcile the README's literal color palette with the tokens-only implementation rule by mapping
  brand values into approved Fluent theme tokens/custom theme slots rather than hard-coded component CSS.
- [x] Approve package-local/offline typography and tabular-number treatment for the current English
  responsive and keynote captures.
- [ ] Validate typography with long localization, Arabic RTL, Japanese, and 200% browser zoom.
- [ ] Define visual state boards for loading, stale CRM, partial evidence, permission, no match, conflict,
  failed bridge action, empty queue, and offline/session-only operation.

### Package and agent identity

- [x] Approve Teams short/full names, declarative-agent name, plugin namespace, publisher/contact,
  descriptions, icons, package title, and solution metadata.
- [x] Verify API plugin v2.4 limits: `name_for_human` <= 20 characters,
  `description_for_human` <= 100, and `description_for_model` <= 2,048.
- [x] Approve ownership of existing solution, feature, app, and placeholder component GUIDs; final
  component GUIDs will come only from Yeoman generation.
- [ ] Approve all six conversation starters or reduce them to the host-supported target count while
  retaining capability exploration as the final starter.
- [x] Freeze and validate the bundle strategy: one shared bundle for all final component entries unless measured evidence
  justifies an isolated heavy experience.

### Existing scaffold disposition

- [x] Remove the untouched generated `DealRoom` placeholder component,
  registration, localization, and placeholder metadata; do not rename it into a final component.
- [x] Record the cleanup in the working tree before generating final identities.
- [x] Confirm no current placeholder source or manifest is treated as an implementation baseline: it
  performs runtime Graph/SharePoint calls, contrary to the offline-first brief.
- [x] Replace README planning status with the locally validated implementation/package status and isolate
  remaining external or unproven gates explicitly.

## Phase 1 - Supported scaffolding and baseline automation

### Generate immutable identities

- [x] Generate each approved Copilot Component with its final name using
  `yo @microsoft/sharepoint --component-type copilotComponent --component-name <FinalName> --framework react --skip-install`.
- [x] Verify all component GUIDs, aliases, tools, schemas, and registrations are
  unique and match the approved catalog.
- [x] Generate `ExploreAgentCapabilities` as its own education/discovery component.
- [x] Configure one shared SPFx bundle with exact manifest coverage and no duplicate membership.

### Pin the supported stack

- [x] Align all `@microsoft/sp-*` dependencies to the exact approved Beta 3 generator output.
- [x] Pin React/ReactDOM `18.3.1`, React types `18.2.79`/`18.2.25`, Fluent React Components `9.74.6`,
  Fluent Icons `2.0.314`, and Griffel React `1.7.7` exactly.
- [x] Remove direct `@fluentui/react` v8 after proving no active v8 imports remain.
- [x] Install Jest and only the focused D3 modules/types selected for the signature visuals.
- [ ] Run `npm ci`, dependency-tree validation, and `heft test --clean` with zero warnings.

### Catalog and release automation

- [x] Create one typed intent catalog with identity, operation, schema, route, education, preview, and
  prompt-routing metadata for every approved tool.
- [x] Add deterministic configure and fail-fast intent validation scripts.
- [x] Add generated-plugin, package-output, persona-media/provenance, intent, and gallery validators.
- [ ] Add the complete React baseline/root-lifecycle validator and generated-media drift workflow required
  by the reusable playbook.
- [x] Replace the build script with the ordered validation/build/package/audit command.
- [ ] Add prompt-routing matrix data shared by validators, demos, and rehearsal.
- [ ] Validate placeholder text, duplicate identity, schema, bundle, localization, registration, plugin
  length, and explorer-threshold failures with focused negative tests.

## Phase 2 - Shared domain graph and deterministic services

### Canonical revenue model

- [ ] Define source contracts for CRM accounts/contacts/opportunities, Microsoft 365 work context,
  pricing/policy, workflow, proposal, forecast, and customer-success outcomes.
- [ ] Define lean canonical view models and pure source-to-view-model mappers.
- [x] Seed at least 80 accounts, 260 contacts, 120 opportunities over eight relative quarters, and the
  connected evidence/policy/action records required by the brief.
- [ ] Make Contoso Global Expansion / `ZDR-2042` coherent across stakeholder, activity, commitment,
  requirement, proposal, commercial, policy, exception, forecast, and handoff records.
- [ ] Use one supplied invocation clock, stable IDs, relative dates, and `Intl` formatting.
- [x] Distinguish verified buyer evidence, seller judgment, deterministic calculation, inference, stale
  evidence, and contrary evidence in the type system.

### Calculation and service boundaries

- [ ] Implement `IRevenueDataService`, `IWorkContextService`, `IPricingAndPolicyService`, and
  `IWorkflowService` with offline mock implementations only.
- [ ] Specify and test qualification, relationship, critical-path/credible-close, commercial, margin,
  approval-authority, probability, forecast-range, and pipeline bridge calculations.
- [x] Implement and test deterministic commercial value, margin, authority, and weighted-forecast
  variance plus account/contact/opportunity referential integrity (3 tests passed, 0 failed).
- [ ] Document assumptions and avoid presenting deterministic demo outputs as predictive facts.
- [ ] Add deterministic referential-integrity, calculation, reset, and no-network tests.

### Session-local action overlay

- [ ] Implement immutable confirmed-action records, stable receipts, subscriptions, guarded
  `sessionStorage`, in-memory fallback, and Reset.
- [ ] Consume the external store with `useSyncExternalStore` and stable snapshots.
- [ ] Prove cross-route updates for commitments, exception decisions, forecast decisions, and handoff.
- [ ] Verify drafts, host context, and transient selection never enter shared persistence.

## Phase 3 - Shared React host and operation contracts

### Host lifecycle and theming

- [x] Implement one persistent React 18 root per entrypoint, reuse on host rerender, and unmount on
  teardown.
- [ ] Add focused StrictMode cleanup, abortable async, and no-update-after-teardown tests.
- [x] Resolve current user synchronously from host context with bundled persona/initials fallback and no
  runtime profile fetch.
- [x] Implement the shared owner-document Griffel renderer, stable one-time Fluent remount, custom
  token theme, responsive light/dark visual system, and portrait-aware shared frame.
- [ ] Add owner-document insertion, forced-colors, and state-preserving theme-flip behavior tests.
- [ ] Add one localized accessible mode-root error boundary and explicit async/event error handling.

### Invocation, continuation, and bridge

- [ ] Implement normalized intent resolvers, compact params, deterministic signatures, property versions,
  and typed transient snapshots owned by each component instance.
- [ ] Preserve passive rerender state and reset only on a fresh normalized property signature.
- [x] Implement host-authoritative Expand and lens-level catalog-owned full-screen destination selection.
- [ ] Implement and test every exact route destination and transferred transient state.
- [x] Implement bounded, deduplicated model-context snapshot calls after committed material state changes.
- [ ] Add focused snapshot signature, no-render-call, safe-draft, and receipt-context tests.
- [x] Implement explicit user-triggered follow-up actions with pending, thrown-error, and `isError` states.
- [ ] Prove no automatic conversation turns, duplicate sends, or sensitive draft leakage with fake-bridge tests.

### Operation dispatchers

- [x] Implement information, review, submit, and education dispatch paths on one shared host.
- [ ] Replace the remaining generic information fallback with a purpose-designed body for every intent.
- [x] Add unique `data-layout` identities and catalog uniqueness tests for the implemented roots.
- [ ] Implement shared mechanics for framing, validation, review actions, confirmation, receipts, and
  queues while keeping domain evidence and consequences purpose-specific.
- [ ] Add a truthful-control inventory and test contract for every retained interaction.

## Phase 4 - Keynote UX proof

### Relationship and risk

- [ ] Implement `GetDealRisk` with evidence provenance, ranking/filter variance, coordinated selection,
  mitigation draft, no-match, and error states.
- [x] Implement `MapBuyingCommittee` with a deterministic portrait-based influence orbit and accessible
  people/evidence equivalent; validate desktop, mobile, light, and dark captures.
- [ ] Add material role/stance filters, graph selection, relationship-owner proposal, and introduction-
  path simulation with focused tests.
- [ ] Prove risk-to-stakeholder context transfer and exact `deal-room/risks` / `deal-room/stakeholders`
  continuation at inline and full-screen widths.

### Mutual close path

- [ ] Implement `BuildMutualActionPlan` Draft -> Validate -> Review -> Confirm -> Receipt -> Reset.
- [ ] Implement the commitment runway with editable buyer/seller milestones, dependency geometry,
  critical path, credible close range, and slip consequence.
- [x] Implement the polished portrait-backed buyer/seller commitment runway with semantic status,
  stable owner rows, critical-path narrative, and responsive visual evidence.
- [ ] Prove prompt-prefill, live draft/review parity, Edit preservation, explicit confirmation, session
  receipt, prompt-backed reset, and exact `deal-room/mutual-plan` continuation.

### Commercial judgment

- [ ] Implement `SimulateCommercialOffer` with materially different named scenarios, inspectable math,
  commercial contour, exact comparison table, and review-request draft.
- [x] Implement live quantity/discount recalculation for value, margin, authority, weighted forecast,
  and a labeled gradient commercial contour; validate the changed-data calculation test and screenshot.
- [x] Replace hand-authored commercial paths with D3 linear scales and monotone line/area generators;
  changed scenario inputs produce different paths and selected coordinates.
- [ ] Implement `ReviewDealException` queue, evidence, alternatives, authority, conditions/rationale,
  confirmation, semantic receipt, updated queue, and preserved filters.
- [ ] Prove selected scenario continuity into `commercial-desk/scenario` and matching evidence into
  `commercial-desk/exception`; simulation must never approve an exception.

### Forecast payoff

- [ ] Implement `InspectForecastCommit` with evidence ledger, critical path, scenario range, category
  consequences, request clarification, manager decision, receipt, and queue update.
- [x] Implement the portrait-backed evidence ledger with verified, contrary, stale, inference, and seller-
  judgment provenance plus selected evidence and responsive/dark visual evidence.
- [x] Implement a D3-scaled signed forecast bridge with exact table equivalent in inline forecast review
  and Revenue Command.
- [ ] Prove confirmed upstream session actions change the visible forecast evidence and movement without
  mutating immutable seeds.

### Keynote gate

- [ ] Capture inline and full-screen evidence at 340, 760, desktop, and keynote widths in light/dark,
  reduced-motion, and 200% zoom states.
- [ ] Rehearse the focused main-stage journey in five minutes with reset, no-network operation, one
  explicit Copilot follow-up, human confirmation, and a fallback path.
- [x] Run focused domain tests plus `heft test --clean` with 3 passes and zero warnings after the premium
  redesign; retain broader lifecycle coverage as open work below.

## Phase 5 - Full-screen revenue application

### Shared shell

- [ ] Implement responsive navigation for My Deals, Deal Room, Commercial Desk, and Revenue Command
  with stable workspace identity, keyboard behavior, destination focus, and narrow-screen adaptation.
- [x] Implement distinct useful default dashboards for My Deals, Deal Room, Commercial Desk, and Revenue
  Command with coordinated D3 visuals, role-specific queues/evidence/policy/interventions, and focused
  invoked-intent continuation where it adds to the workspace.
- [x] Keep Deal Room as the keynote workspace and coordinate the selected Contoso opportunity,
  stakeholder graph, evidence ledger, commitment runway, people, and credible-close narrative across its
  current composition.
- [ ] Keep Commercial Desk queue/scenario-led and Revenue Command forecast/intervention-led rather than
  duplicating a regional KPI dashboard.
- [ ] Implement the isolated `education/capabilities` route outside operational navigation.

### Exact continuation

- [ ] Implement and test every catalog destination from the README.
- [ ] Preserve entity, filters, comparison set, selected evidence, safe what-if values, draft, and review
  step where each intent contract requires them.
- [ ] Test passive rerender vs fresh invocation and representative transient transfer for information,
  review, submit, and education models.
- [ ] Prove full-screen destinations add context and operations rather than enlarging inline layouts.

## Phase 6 - Remaining operational catalog

### Understand, discover, and engage

- [ ] Implement and validate `BuildAccountBrief`, `DiscoverAccountOpportunity`,
  `PrepareCustomerMeeting`, and `TrackMeetingFollowUp`.
- [ ] Implement and validate `QualifyOpportunity`, `ReviewMeetingCommitments`, and their purpose-specific
  decision workflows.

### Strategy, proposal, and value

- [ ] Implement and validate `CoachDealStrategy`, `ResearchCompetitivePosition`,
  `CreateExecutiveEngagementPlan`, and `BuildValueCase`.
- [ ] Implement and validate `ShapeSolutionProposal` and `ReviewProposalReadiness`, including claim
  provenance, readiness findings, conditions, and cross-functional ownership.

### Leadership and handoff

- [ ] Implement and validate `ExplorePipelineQuality` with material filter/metric variance, forecast
  bridge coordination, exact values, and named intervention.
- [ ] Implement and validate `PlanCustomerSuccessHandoff` with sold outcomes, risks, owners, first-value
  dependencies, review, receipt, and no automatic opportunity closure.
- [ ] Run every information default/change/detail/no-match/error test, every review lifecycle, and every
  submit lifecycle before marking the catalog complete.

## Phase 7 - Capability explorer and message-driven journey

### Capability education

- [ ] Populate complete catalog-owned education metadata and deterministic safe preview properties for
  all 20 operational intents; exclude the explorer itself.
- [ ] Implement category navigation, search, audience/outcome/operation filters, selected detail, prompt
  copy, feature-detected host launch fallback, Previous/Next, and a featured tour.
- [ ] Implement isolated read-only preview boundaries; review/submit previews stop before confirmation
  and display `Demo preview - no action applied`.
- [ ] Validate threshold rules, counts, keyboard behavior, copy success/failure, preview reset, every
  preview smoke, no network/writes, no nested headers, and no overflow.

### Message-driven update demonstration

- [ ] Choose the domain-specific update tool and exact stage transition for the required message-driven
  demo; do not invent a ceremonial status tool.
- [ ] Publish current deal/route/evidence/next-action context, send a user-triggered follow-up, route the
  fresh invocation, prefill but do not submit, and complete Review -> Confirm -> Receipt explicitly.
- [ ] Verify model-context deduplication, safe payloads, bridge errors, and actual host routing separately
  from tenant-free tests.

## Phase 8 - Showcase polish, accessibility, and evidence

### Visual and interaction polish

- [ ] Add purposeful 180-300 ms state transitions and staggered entry with static Griffel classes;
  reduced motion renders final geometry immediately.
- [ ] Audit token contrast, status semantics, focus, labels, long text, RTL, currency, fiscal periods,
  time zones, names, addresses, and regional policy packs.
- [ ] Audit every control for a tested effect on data, geometry, calculation, evidence, draft, or stage;
  remove unsupported affordances.
- [ ] Verify all charts have title/description, exact visible values, keyboard activation where relevant,
  insight text, and table/list equivalence.

### Tenant-free visual harness

- [x] Build a parameterized tenant-free harness that renders every catalog intent in inline/full-screen,
  light/dark, variable viewport, and reduced-motion states.
- [x] Automate runtime, console, horizontal-overflow, and broken-image checks for the 13 publication
  captures; save current screenshot hashes and layout identities in `assets/gallery-evidence.json`.
- [ ] Automate accessible-name, chart, keyboard, focus, 200%-zoom, and blank/error-state
  checks for every catalog intent and full-screen dashboard.
- [ ] Save one approved screenshot per intent plus dashboard/category/receipt states and a machine-readable
  evidence matrix under `ux-review/evidence/`.
- [x] Add deterministic gallery capture with animation disabled and stable host clipping.

### Tenant host gate

- [ ] **BLOCKED: tenant domain/authentication** Validate Workbench CSP, host-authoritative display mode,
  bridge follow-up/context, iframe focus restoration, forced colors, and screen-reader output in an
  authenticated tenant; record exact environment and results.
- [ ] **BLOCKED: media-rights approval** Confirm public redistribution rights for the seven standard
  demo-persona portraits before GitHub publication, or replace them with rights-cleared equivalents and
  regenerate provenance, screenshots, release evidence, and the `.sppkg`.

## Phase 9 - Documentation, demos, packaging, and release

### Documentation and demo assets

- [x] Replace README planning status with implementation status and add PnP Summary, Screenshots,
  Applies to, Prerequisites, Minimal Path to Awesome, Features, safety, accessibility/global scope,
  validation, solution structure, references, author, and version history sections.
- [x] Create `assets/sample.json` and validate all referenced publication PNGs, metadata, URLs, order,
  dimensions, signatures, and alt text.
- [x] Create the 4-minute keynote, 10-minute business journey, 5-minute technical demo, designer review,
  and generated 21-tool routing matrix with setup, reset, guardrails, collision pairs, and fallback paths.
- [ ] Rehearse all three demo cuts in an authenticated tenant and record actual prompt routing, extracted
  properties, display-mode continuation, timing, and fallback results.
- [x] Document capability-explorer maintenance, mock/live service boundaries, formulas, limitations,
  telemetry/outcome plan, support owner, and external tenant prerequisite.

### Production and package evidence

- [x] Run the canonical clean production build and complete test suite with zero warnings.
- [x] Validate the generated API plugin v2.4 and 21 functions inside the agent ZIP.
- [x] Audit the final `.sppkg`: hashed production JavaScript only, shared bundle entry coverage, no stale
  output, no duplicate substantial media/base64 catalog, no Fluent icon font, and recorded size metrics.
- [ ] Commit only the ready-to-deploy `.sppkg` from package output; keep debug/build artifacts ignored.
- [x] Record test totals, warning count, artifact names/sizes/hashes, JS count/total/largest,
  media duplicate counts, diagnostics, and `git diff --check` results.
- [x] Generate and validate `assets/release-evidence.json` from the final package, agent ZIP, configured
  bundle/component counts, media, and 13-capture visual evidence.
- [ ] Run a clean-clone/offline rehearsal, inspect the current agent ZIP, stop temporary servers, and
  obtain release approval.

## Deferred - Dynamic data / API integration

- [ ] Implement authenticated CRM/CPQ, Graph/Work IQ, SharePoint, policy, workflow, approval, and
  e-signature adapters behind the validated service contracts.
- [ ] Add production authorization, territory access, pricing authority, revenue recognition, legal,
  retention, residency, audit, throttling, consent, telemetry, and conflict requirements.
- [ ] Replace session receipts with real commands only after explicit environment-specific review and
  preserve the same view-model and human-confirmation contracts.
- [ ] Revalidate tool schemas, provisioning, CSP, permissions, host behavior, accessibility, and global
  policy packs against each approved live system.

## Docs and cleanup

- [x] Reconcile this tracker after the premium UX/media redesign using refreshed executable evidence.
- Keep at most one **IN PROGRESS** implementation item unless parallel work is explicit.
- [x] Split locally proven work from tenant-host prerequisites; do not mark external checks complete from
  the tenant-free harness.
- [ ] Reconcile stale phase wording after redesign or scale-out instead of leaving contradictory history.
- [x] Update all duplicated counts and package metrics from the latest saved command output only.
- [x] Do not create a competing status document; evidence files supplement this tracker.

## Reusable playbook

The mandatory engineering, UX, safety, automation, and packaging criteria remain in
[agentic-creation-rules.md](agentic-creation-rules.md). This tracker specializes those rules for the
Revenue Deal Room and records approved exceptions. No exception is currently approved.

## Open decisions

- [x] ~~Is the 20+1 catalog approved as final?~~ **Approved:** 20 operational tools plus one capability
  explorer are generated and catalog-validated; purpose-specific implementation depth remains tracked.
- [x] ~~Is the README textual design system sufficient?~~ **Resolved:** the README plus the named Zava
  Innovation Hub source/screenshots became the visual contract; premium captures now provide evidence.
- [ ] Which 5-6 beats form the main-stage keynote, and which belong only in the extended demo?
- [x] ~~Is `Zava Revenue Deal Room` the final identity?~~ **Approved:** product/agent/package use Zava
  Revenue Deal Room and plugin-facing `Zava Revenue Room` validates at 17 characters.
- [ ] Are six conversation starters supported by the target host, or should this sample use three with
  capability exploration last?
- [x] ~~May the untouched `DealRoom` placeholder be removed?~~ **Approved and completed** before final
  Yeoman generation; no placeholder identity or live starter code remains active.
- [x] ~~Decide whether to use the installed focused D3 modules.~~ **Resolved:** D3 array/scale/shape/geo
  plus TopoJSON and bundled Natural Earth data now drive commercial, forecast, pipeline, relationship,
  and regional-map geometry; React owns marks, state, interaction, and accessibility.
- [ ] What tenant and test personas will be used for the authenticated Workbench gate?