import fs from 'node:fs';import path from 'node:path';import unzipper from 'unzipper';
const root=path.resolve(import.meta.dirname,'..'),zip=path.join(root,'teams','zava-procurement-command-center.zip');
if(!fs.existsSync(zip))throw new Error('Generated agent ZIP is missing');const archive=await unzipper.Open.file(zip),entry=archive.files.find(file=>file.path.endsWith('ai-plugin.json'));
if(!entry)throw new Error('Generated ai-plugin.json is missing');const plugin=JSON.parse((await entry.buffer()).toString('utf8'));
const failures=[];if(plugin.schema_version!=='v2.4')failures.push('schema is not v2.4');if(plugin.name_for_human.length>20)failures.push('human name exceeds 20 characters');if((plugin.description_for_human||'').length>100)failures.push('human description exceeds 100 characters');if((plugin.functions||[]).length!==22)failures.push(`expected 22 functions, found ${(plugin.functions||[]).length}`);if(new Set((plugin.functions||[]).map(fn=>fn.name)).size!==22)failures.push('duplicate generated function');if(failures.length)throw new Error(failures.join('; '));console.log('Generated plugin validation passed: v2.4, 22 unique functions, metadata limits valid.');